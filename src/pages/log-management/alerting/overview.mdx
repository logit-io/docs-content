---
title: Overview
metaTitle: Learn How to Configure Alerts for Logs
description: Discover how alerting works on Logit.io with managed ElastAlert 2 YAML rules, rule types, notification destinations, and optional OpenSearch Alerting monitors.
stackTypes: logs
---

# An introduction to alerting

## Alerting and notifications

Proactive alerting helps you catch production errors, unusual log volumes, and security-relevant events before they escalate. On Logit.io you configure **ElastAlert 2** rules as YAML from the dashboard, test them, and apply updates when you are ready. Notifications can go to email, chat tools, on-call systems, webhooks, and [many other destinations](/log-management/alerting/destinations).

You express what to match using the same OpenSearch query patterns you use elsewhere; rules run on **managed** infrastructure—you do not configure cluster endpoints inside the rule file.

<Callout type="info">
 Automate enable/disable and rule CRUD with the [Developer API](/developer-api/alerting-via-api).
</Callout>

## Primary workflow (YAML editor)

1. Enable alerting under **Alerting & Notifications** for your Logs stack.
2. Create or open a rule and edit the YAML.
3. Run **Test only** to check parsing and query results.
4. Choose **Update** when the rule is ready.

See [How do I create a new alerting rule?](/log-management/alerting/creating-alerts) for step-by-step UI guidance.

## Subject, body, and context

- [Subject & body](/log-management/alerting/alert-subject-and-body) — titles, message text, Python and Jinja formatting, multiple destinations.
- [Context & links](/log-management/alerting/alert-context-and-links) — top field counts, `include`, OpenSearch Discover URLs.

## Rule types (ElastAlert 2)

Each rule sets `type:` to one of the built-in rule types. Every type has its own required fields and options:

| Type | Summary |
|------|---------|
| [any](/log-management/alerting/rule-types/any) | Alert on every document that matches the filter. |
| [blacklist](/log-management/alerting/rule-types/blacklist) | Match when a field value is in a deny list. |
| [whitelist](/log-management/alerting/rule-types/whitelist) | Match when a field value is **not** in an allow list. |
| [change](/log-management/alerting/rule-types/change) | Match when a field changes between events sharing a `query_key`. |
| [frequency](/log-management/alerting/rule-types/frequency) | Match when at least `num_events` occur inside `timeframe`. |
| [spike](/log-management/alerting/rule-types/spike) | Match when volume in the current window spikes or drops vs the previous window. |
| [flatline](/log-management/alerting/rule-types/flatline) | Match when there are fewer than `threshold` events in `timeframe`. |
| [new_term](/log-management/alerting/rule-types/new-term) | Match when a new value appears in a monitored field. |
| [cardinality](/log-management/alerting/rule-types/cardinality) | Match on unique-count of a field above/below limits. |
| [metric_aggregation](/log-management/alerting/rule-types/metric-aggregation) | Match when a metric (avg, max, sum, etc.) crosses a threshold. |
| [spike_aggregation](/log-management/alerting/rule-types/spike-aggregation) | Match when a metric spikes or drops between windows. |
| [percentage_match](/log-management/alerting/rule-types/percentage-match) | Match when a subset of documents exceeds or falls below a percentage of the whole. |

Browse all guides under [Rule types](/log-management/alerting/rule-types).

## Destinations

Set `alert:` to one or more destinations (for example `email`, `slack`, or custom webhooks via `post` / `post2`). Each destination has its own configuration keys. Browse [Destinations](/log-management/alerting/destinations) for a full list with examples.

## Other useful features

- OpenSearch Dashboards links in alert content (where configured).
- Top counts and field values in alert bodies.
- Aggregation windows and scheduled digest-style sending (`aggregation`).
- Per-key bucketing with `query_key` to reduce noise.
- `realert` to control how often the same rule can fire.

### Example frequency rule (managed stack)

```yaml copy
name: High error rate example
type: frequency
index: "*-*"
num_events: 50
timeframe:
  minutes: 15
filter:
  - query:
      query_string:
        query: "log.level:error OR level:error"
alert:
  - "email"
email:
  - "alerts@example.com"
```

## ElastAlert execution logs in your stack

Logit.io runs ElastAlert 2 for you, but **execution output is still written into your Logs stack** (the **elastalert** index). That helps you audit rule behaviour, see test runs, and debug cases where matches occur but no notification arrives.

To view those messages in the dashboard, open **Diagnostic logs** for your stack at **`/logs-settings/logfile`** (stack logs). Use your usual Logs search or Discover workflow against the elastalert data alongside application logs.

## OpenSearch Alerting (optional)

If you prefer **OpenSearch Alerting** (monitors in OpenSearch Dashboards) instead of YAML rules, see [OpenSearch Alerting](/log-management/alerting/opensearch-alerting-monitors).

## Best getting started guides

Start with these guides in order:

1. **[Validate your rule first](/log-management/alerting/guides/validating-alert-rules)**  
   Confirm YAML structure, required fields, and destination settings before troubleshooting behaviour.
2. **[Send alerts for common scenarios](/log-management/alerting/guides/sending-alert-notifications)**  
   Use ready-to-adapt examples for spike, flatline, and per-host/application alerting patterns.
3. **[Set a log-volume threshold warning](/log-management/alerting/guides/set-alert-volume-to-prevent-exceeding-stack-limit)**  
   Warn early when ingestion approaches your stack limit using a frequency rule.
4. **[Configure high CPU notifications (OpenSearch Alerting)](/log-management/alerting/guides/high-cpu-alerts)**  
   Build a monitor in OpenSearch Dashboards when you prefer monitor-based alerts.

More task-focused walkthroughs are available under [Guides](/log-management/alerting/guides), including [HTML in email alerts](/log-management/alerting/guides/using-html-in-email-alerts).

## Frequently asked questions

### My rule is not getting any hits?

If tests show `0` hits, confirm the **index pattern** (for example `*-*` or your Beat prefix) matches documents in Discover. Temporarily simplify or remove `filter` to see whether any documents are seen at all.

Typical filter using query string syntax:

```yaml copy
filter:
  - query:
      query_string:
        query: "foo:bar AND baz:abc*"
```

If you use a `term` query on an analysed field, the token in the index may differ from the raw string (for example use a `.keyword` field if your mapping provides one).

### I got hits, why did I not get an alert?

Behaviour depends on `type:`. For `any`, each hit can be a match. For `frequency`, `num_events` must occur inside `timeframe`. For `spike` and `flatline`, windowing and thresholds must be satisfied.

Example frequency snippet (no cluster settings):

```yaml copy
name: Log frequency rule example
type: frequency
index: "*-*"
num_events: 30
timeframe:
  minutes: 5
filter:
  - query:
      query_string:
        query: "*"
alert:
  - "email"
email:
  - "test@example.com"
```

If you see matches but `0 alerts sent`, check **aggregation** settings, **silence** / `realert`, and rule logs for messages such as `Ignoring match for silenced rule`.

### Why only one alert when I expected several?

`realert` sets the minimum time between alerts for the same rule. To allow every match:

```yaml copy
realert:
  minutes: 0
```

### How can I reduce duplicate alerts?

Raise `realert` or combine with `query_key` so duplicates are scoped per field value:

```yaml copy
realert:
  hours: 8
query_key: user
```

### Send a digest on a schedule

```yaml copy
aggregation:
  schedule: "2 4 * * mon,fri"
```

### Custom timestamp field

If events do not use `@timestamp`, set:

```yaml copy
timestamp_field: log-time
timestamp_type: iso
```

You can also tune `query_delay` and `buffer_time` for late-arriving data.
