---
title: Kibana
pageTitle: Kibana Logging Configuration
subTitle: Ship Kibana log data to your hosted Logit.io Stack
logo: kibana
color: "#d7689d"
description: Filebeat allows you to send logs to your ELK stacks. Configure Filebeat to send Kibana logs to Logstash and Elasticsearch.  
stackTypes: logs
sslPortType: beats-ssl
---

Filebeat is an open source shipping agent that lets you ship logs from local files to one or more destinations, 
including Logstash and Elasticsearch.

<Steps>

  ### Install Filebeat

  <InstallFilebeat />

  ### Enable the Kibana module

  <Tabs items={["Windows", "Linux", "macOS", "DEB", "RPM"]}>
    <Tab>
    There are several built in filebeat modules you can use. You will need to enable the kibana module:

    ```ps1 copy
    .\filebeat.exe modules list
    .\filebeat.exe modules enable kibana
    ```
    In the module config under modules.d, change the module settings to match your environment. You must enable at least one fileset in the module. 
    
    ___Filesets are disabled by default___.

    Copy the snippet below and replace the contents of the kibana.yml module file:

    ```yaml copy
    # Module: kibana
    # Docs: https://www.elastic.co/guide/en/beats/filebeat/8.12/filebeat-module-kibana.html

    - module: kibana
      # Server logs
      log:
        enabled: true

        # Set custom paths for the log files. If left empty,
        # Filebeat will choose the paths depending on your OS.
        #var.paths:

      # Audit logs
      audit:
        enabled: true

        # Set custom paths for the log files. If left empty,
        # Filebeat will choose the paths depending on your OS.
        #var.paths:
    ```
    </Tab>
    <Tab>
      There are several built in filebeat modules you can use. You will need to enable the kibana module:

      ```ps1 copy
      sudo filebeat modules list
      sudo filebeat modules enable kibana
      ```

      In the module config under modules.d, change the module settings to match your environment. You must enable at least one fileset in the module. 
      
      ___Filesets are disabled by default___.

      Copy the snippet below and replace the contents of the kibana.yml module file:

      ```yaml copy
      # Module: kibana
      # Docs: https://www.elastic.co/guide/en/beats/filebeat/8.12/filebeat-module-kibana.html

      - module: kibana
        # Server logs
        log:
          enabled: true

          # Set custom paths for the log files. If left empty,
          # Filebeat will choose the paths depending on your OS.
          #var.paths:

        # Audit logs
        audit:
          enabled: true

          # Set custom paths for the log files. If left empty,
          # Filebeat will choose the paths depending on your OS.
          #var.paths:
      ```
    </Tab>

    <Tab>
      There are several built in filebeat modules you can use. You will need to enable the kibana module:

      ```ps1 copy
      ./filebeat modules list
      ./filebeat modules enable kibana
      ```

      In the module config under modules.d, change the module settings to match your environment. You must enable at least one fileset in the module. 
      
      ___Filesets are disabled by default___.

      Copy the snippet below and replace the contents of the kibana.yml module file:

      ```yaml copy
      # Module: kibana
      # Docs: https://www.elastic.co/guide/en/beats/filebeat/8.12/filebeat-module-kibana.html

      - module: kibana
        # Server logs
        log:
          enabled: true

          # Set custom paths for the log files. If left empty,
          # Filebeat will choose the paths depending on your OS.
          #var.paths:

        # Audit logs
        audit:
          enabled: true

          # Set custom paths for the log files. If left empty,
          # Filebeat will choose the paths depending on your OS.
          #var.paths:
      ```
    </Tab>

    <Tab>
      There are several built in filebeat modules you can use. You will need to enable the kibana module:

      ```ps1 copy
      sudo filebeat modules list
      sudo filebeat modules enable kibana
      ```
      In the module config under modules.d, change the module settings to match your environment. You must enable at least one fileset in the module. 
      
      ___Filesets are disabled by default___.

      Copy the snippet below and replace the contents of the kibana.yml module file:

      ```yaml copy
      # Module: kibana
      # Docs: https://www.elastic.co/guide/en/beats/filebeat/8.12/filebeat-module-kibana.html

      - module: kibana
        # Server logs
        log:
          enabled: true

          # Set custom paths for the log files. If left empty,
          # Filebeat will choose the paths depending on your OS.
          #var.paths:

        # Audit logs
        audit:
          enabled: true

          # Set custom paths for the log files. If left empty,
          # Filebeat will choose the paths depending on your OS.
          #var.paths:
      ```
    </Tab>

    <Tab>
      There are several built in filebeat modules you can use. You will need to enable the kibana module:

      ```ps1 copy
      sudo filebeat modules list
      sudo filebeat modules enable kibana
      ```
      In the module config under modules.d, change the module settings to match your environment. You must enable at least one fileset in the module. 
      
      ___Filesets are disabled by default___.
      
      Copy the snippet below and replace the contents of the kibana.yml module file:

      ```yaml copy
      # Module: kibana
      # Docs: https://www.elastic.co/guide/en/beats/filebeat/8.12/filebeat-module-kibana.html

      - module: kibana
        # Server logs
        log:
          enabled: true

          # Set custom paths for the log files. If left empty,
          # Filebeat will choose the paths depending on your OS.
          #var.paths:

        # Audit logs
        audit:
          enabled: true

          # Set custom paths for the log files. If left empty,
          # Filebeat will choose the paths depending on your OS.
          #var.paths:
      ```
    </Tab>
  </Tabs>

  ### Update your configuration file

  <UpdateFilebeatConfigFile />

  ### Validate Configuration

  <ValidateBeat beatname="filebeat" />

  ### Check Logit.io for your logs
  <LaunchStack utmMedium="logs" utmCampaign="kibana" source="kibana" />

  ### How to diagnose no data in Stack

  <DiagnoseNoData />


</Steps>